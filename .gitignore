node_modules/
dist/
*.tsbuildinfo
*.log

# Generated by scripts/sync-protocol.mjs on every build — shared/protocol.ts is
# the source of truth, these two are copies.
sidecar/src/protocol.ts
plugin/protocol.ts

# Local secrets and machine-specific state. The bridge token lives in the
# sidecar's config dir, not here, but keep these out by reflex anyway.
.env
token
local.config.json
